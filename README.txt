MiniOS Setup Instructions

1. Open WSL and navigate to the mini-os directory.
2. Run `make` to build everything.
3. Run `make run` to launch the OS in QEMU.
4. Use `make clean` to clean up the build.
